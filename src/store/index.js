import Vue from 'vue'
import Vuex from 'vuex'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
    // demo: ''
  },
  mutations: {
    // SET_DEMO(state, data) {
    //   state.projectList = data
    // }
  },
  actions: {
    // demo
    // async getMessageUnreadCount({ commit }) {
    //   getUserUnReadMessageCount().then(res => {
    //     commit('SET_MESSAGE_UNREAD_COUNT', res.data)
    //   })
    // }
  },
  modules: {}
})
